// import { AddAnimal } from '../simple-store/simple-store.component';
// import { Store } from '@ngxs/store';

// export class DispatchingActions {

//   constructor(private store: Store) {}

//   // To call an action we inject the store in the constructor of our component
//   // and then dispatch a new instance of the action
//   dispatchAction(name: string) {
//     this.store.dispatch(new AddAnimal(name));
//   }

//   // Multiple actions can be dispatched by passing them into an array
//   dispatchMultipleActions(name: string) {
//     this.store.dispatch([
//       new AddAnimal(name),
//       new AddAnimal(name)
//     ]);
//   }



// }
